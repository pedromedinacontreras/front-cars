// import { fromJS } from 'immutable';
// import { makeSelectReportDomain } from '../selectors';

// const selector = makeSelectReportDomain();

describe('makeSelectReportDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
