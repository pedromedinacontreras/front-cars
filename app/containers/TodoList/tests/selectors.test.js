// import { fromJS } from 'immutable';
// import { makeSelectTodoListDomain } from '../selectors';

// const selector = makeSelectTodoListDomain();

describe('makeSelectTodoListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
